{
  "description": "ACE Plugin Hooks - ACE trigger enforcement, gitignore management, version checking, auto-update, and execution logging",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚨 ACE SYSTEM ACTIVE: Skills ace-playbook-retrieval (BEFORE work) and ace-learning (AFTER work) auto-trigger on keywords: implement, build, create, update, modify, fix, debug, refactor, optimize, integrate, configure, setup, deploy, test, verify. Check EVERY user message for trigger words.'",
            "timeout": 1
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/ensure-gitignore.sh",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/check-ace-version.sh",
            "timeout": 3
          },
          {
            "type": "command",
            "command": "[ -f ~/.ace/auto-update-enabled ] && ${CLAUDE_PLUGIN_ROOT}/scripts/ace-claude-init.sh --auto-update || true",
            "timeout": 10
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '⚠️ ACE TRIGGER CHECK: If this message contains trigger words (implement|build|create|update|modify|fix|debug|refactor|optimize|integrate|configure|setup|deploy|test|verify|validate|add|develop|write|change|edit|enhance|extend|revise|troubleshoot|resolve|diagnose|improve|restructure|connect|install|architect|design|plan|migrate|upgrade), invoke ace-playbook-retrieval skill BEFORE other tools, then ace-learning skill AFTER completing work.'",
            "timeout": 1
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p ~/.ace && echo \"{\\\"timestamp\\\":\\\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\\\",\\\"tool\\\":\\\"Bash\\\",\\\"exit_code\\\":\\\"$EXIT_CODE\\\"}\" >> ~/.ace/execution_log.jsonl"
          }
        ]
      }
    ]
  }
}
