{
  "mcpServers": {
    "ace-pattern-learning": {
      "command": "npx",
      "args": [
        "--yes",
        "@ce-dot-net/ace-client@3.7.2",
        "--config",
        "${XDG_CONFIG_HOME:-${HOME}/.config}/ace/config.json",
        "--projectID",
        "${ACE_PROJECT_ID}"
      ]
    }
  }
}
